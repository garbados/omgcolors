// Generated by CoffeeScript 1.6.2
(function() {
  var b_phase, center, color_body, colors, frequency, g_phase, get_rgb, index, phase, rgb2color, sign, width, _i, _results;

  colors = (function() {
    _results = [];
    for (_i = 0; _i <= 32; _i++){ _results.push(_i); }
    return _results;
  }).apply(this);

  index = 0;

  sign = 1;

  phase = 2 * Math.PI;

  g_phase = phase / 3;

  b_phase = g_phase * 2;

  frequency = 0.3;

  width = 255 / 2;

  center = 255 / 2;

  rgb2color = function(r, g, b) {
    return "rgb(" + r + ", " + g + ", " + b + ")";
  };

  get_rgb = function(i) {
    var x, _j, _len, _ref, _results1;

    if (i == null) {
      i = index;
    }
    _ref = [0, g_phase, b_phase];
    _results1 = [];
    for (_j = 0, _len = _ref.length; _j < _len; _j++) {
      x = _ref[_j];
      _results1.push(Math.floor((Math.sin((frequency * i) + x) * width) + center));
    }
    return _results1;
  };

  color_body = function() {
    var color;

    color = rgb2color.apply(null, get_rgb());
    $('body').css('background-color', color);
    if ((index >= colors.length) || (index < 0)) {
      sign = sign * -1;
    }
    return index += sign;
  };

  $(function() {
    return setInterval(color_body, phase * 10);
  });

}).call(this);
